# Configuración del servidor Flask-SocketIO
SECRET_KEY=ThVNKaibkNX2_VwKDxqoIQR-92sey90ULukVy3--uM8
FLASK_ENV=production

# CORS: dominios permitidos separados por coma
# Desarrollo: * (permite todos)
# Producción: lista tus dominios exactos
CORS_ALLOWED_ORIGINS=https://tuapp.com,https://www.tuapp.com

# Puerto del servidor
PORT=5000

# Host (0.0.0.0 para acceso externo, 127.0.0.1 solo local)
HOST=0.0.0.0

# SSL/TLS - Certificados (OPCIONAL - usa solo si NO usas Nginx)
# Deja vacío si usas Nginx como proxy inverso (RECOMENDADO)
SSL_CERT_FILE=
SSL_KEY_FILE=

# Base de datos PostgreSQL
# Formato: postgresql://usuario:password@host:puerto/nombre_bd
DATABASE_URL=postgresql://postgres:tu_password_aqui@localhost:5432/upred_db

# Alternativa: mismos DB_* que usa la API
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password_aqui
DB_NAME=upred_db
