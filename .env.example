# ========================================
# CONFIGURACIÓN DEL SERVIDOR WEBSOCKET
# ========================================

# Clave secreta para Flask (CAMBIAR EN PRODUCCIÓN)
# Genera una con: python generate_secret.py
SECRET_KEY=super-secret-key-change-me-in-production

# Entorno (development o production)
FLASK_ENV=production

# Configuración de CORS
# Para desarrollo: * (permite todos los orígenes)
# Para producción: lista de orígenes permitidos separados por coma
# Ejemplo: https://tupagina.com,https://app.tupagina.com
CORS_ALLOWED_ORIGINS=https://tuapp.com,https://www.tuapp.com

# Puerto y host del servidor WebSocket
PORT=5000
HOST=0.0.0.0

# SSL/TLS - Certificados (OPCIONAL - usa solo si NO usas Nginx)
# Deja vacío si usas Nginx como proxy inverso (RECOMENDADO)
SSL_CERT_FILE=
SSL_KEY_FILE=

# ========================================
# BASE DE DATOS MYSQL (MISMA QUE LA API)
# ========================================

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=tu_password_aqui
DB_NAME=upred_db

# ========================================
# NOTAS IMPORTANTES:
# ========================================
# - Esta configuración debe conectarse a la MISMA base de datos que tu API
# - Asegúrate de que la base de datos tiene las tablas: salas_chat, mensajes, 
#   destinatarios_mensaje, grupos, miembros_grupo, usuarios
# - El usuario de BD debe tener permisos de INSERT, UPDATE, SELECT
# - En producción, cambia SECRET_KEY por una clave aleatoria segura
# - En producción, configura CORS_ALLOWED_ORIGINS con tus dominios específicos
# - Para conectar al WebSocket: ws://HOST:PORT?user_id=TU_USER_ID
